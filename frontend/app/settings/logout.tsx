import { Text, View } from "react-native";

export default function matches() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Edit settings/logout.tsx to edit this screen.</Text>
    </View>
  );
}
